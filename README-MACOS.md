Production CleanFlow package only.

No demo data, tests, Windows shortcuts, recordings, or sample records are included.

For the Mac AI: open this folder in Codex on macOS and build an installable .app with assets/cleanflow-icon.png. Do not include demo mode. Test staff, apartments, planning, confirmation, payments, backup, and restore.

For local development only: node server.mjs then open http://127.0.0.1:5173. The final Mac app must not require Node for the user.
